{
  "name": "sygnal-calculator",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.12"
  },
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "tsc && vite build --emptyOutDir",
    "gh-pages": "gh-pages -d dist -b gh-pages",
    "deploy": "npm run build && npm run gh-pages"
  },
  "cca": {
    "language": "javascript",
    "streamLib": "xstream"
  },
  "dependencies": {
    "sygnal": "^4.0.2"
  }
}
